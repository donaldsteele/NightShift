using Avalonia.Controls;

namespace NightShift.Desktop.Views;

/// <summary>plan.md §9.2. Purely declarative — the debounce and every write live in the view model.</summary>
public partial class SettingsView : UserControl
{
    public SettingsView()
    {
        InitializeComponent();
    }
}
